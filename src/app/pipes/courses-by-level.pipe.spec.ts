import { CoursesByLevelPipe } from './courses-by-level.pipe';

describe('CoursesByLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new CoursesByLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
